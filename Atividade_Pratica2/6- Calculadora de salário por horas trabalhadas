numero_funcionario = int(input())
hrs_trabalhadas = int(input())
valor_por_hora = float(input())

salario = hrs_trabalhadas * valor_por_hora

print(f'\nNúmero do funcionário = {numero_funcionario}')
print(f'Salário = R$ {salario:.2f}')
